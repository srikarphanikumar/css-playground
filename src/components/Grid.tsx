import React from 'react';

const Grid = () => {
    return (
        <div>
            <h2 className="text-2xl font-bold mb-4">Grid</h2>
            <p>This section will contain various Grid created with CSS.</p>
        </div>
    );
};

export default Grid;